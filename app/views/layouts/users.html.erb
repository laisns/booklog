<!DOCTYPE html>
<html>
  <head>
    <title>Booklog</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'users', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'users', 'data-turbolinks-track': 'reload' %>

    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body class="hold-transition sidebar-mini">
    <div class="wrapper bg-old-blue">
      <%= render partial: 'layouts/navbar' %>
      <%= render partial: 'layouts/flash_messages' %>

      <div class="wrapper">
        <%= render partial: 'layouts/content_header' %>
      </div>

      <%= yield %>

      <footer class="bg-old-blue text-white alternative-font">
        <div class="float-right d-none d-sm-block"><b>Version</b> 3.0.5</div>
        <strong>Copyright &copy; 2020 <a href="#" class="text-cyan">BookLog</a>.
        </strong> All rights reserved.
      </footer>

      <aside class="control-sidebar control-sidebar-dark">
      <!-- Control sidebar content goes here -->
      </aside>
    </div>
  </body>
</html>
